import {notFound} from "next/navigation"; import {exams} from "@/lib/exams";
export function generateStaticParams(){return exams.map(e=>({slug:e.slug}))}
export default function ExamPage({params}:{params:{slug:string}}){const e=exams.find(x=>x.slug===params.slug);if(!e)notFound();return <main style={{maxWidth:1000,margin:"auto",padding:24,fontFamily:"system-ui"}}><a href="/">← SarkariHub</a><h1>{e.name}</h1><p>{e.organization} • {e.category}</p><hr/><h2>Eligibility</h2><p>Age: {e.minAge}–{e.maxAge} • Qualification: {e.qualifications} • Categories: {e.categories.join(", ")}</p><h2>Important Dates</h2><p>Exam: {e.examDate} • Application last date: {e.lastDate}</p><h2>Vacancies</h2><p>{e.vacancies}</p><h2>Salary</h2><p>{e.salary}</p><p><a href={e.officialUrl} target="_blank" rel="noreferrer" style={{fontWeight:800}}>Visit official website →</a></p><p style={{color:"#64748b"}}>Always verify the latest notification before applying.</p></main>}